{
  "name": "cineclube",
  "version": "1.0.0",
  "private": true,
  "description": "App de avaliação de filmes do cineclube — catálogo via TMDB, avaliações persistidas em SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
