# O endereço do servidor, para o build que vai dentro do aplicativo. O site não
# usa nada disto: lá a API é a própria origem.
#
# Copie para .env.app (que não é versionado) e ponha o seu.
VITE_API_BASE=https://seu-servidor
